definitions:
  res.Token:
    properties:
      expire_at:
        type: string
      token:
        type: string
    type: object
info:
  contact: {}
paths:
  /api/sync:
    post:
      consumes:
      - application/json
      description: 全ての顧客のデータを同期します
      produces:
      - application/json
      responses:
        "200":
          description: 全顧客同期完了
          schema:
            type: string
        "500":
          description: 内部サーバーエラー
          schema:
            type: string
      summary: 全顧客データ同期
      tags:
      - sync
  /api/token:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: トークン情報
          schema:
            $ref: '#/definitions/res.Token'
        "500":
          description: 内部サーバーエラー
          schema:
            type: string
      summary: トークンを取得します。
      tags:
      - token
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: 更新完了
          schema:
            type: string
        "500":
          description: 内部サーバーエラー
          schema:
            type: string
      summary: トークンを保存します。
      tags:
      - token
swagger: "2.0"
